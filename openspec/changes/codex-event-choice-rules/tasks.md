## 1. 事件协议与目录匹配

- [x] 1.1 Bridge 增加可选 `eventId/pageId/optionId` 并保持反射失败安全；用 Mod 形状断言确认字段与兼容注释存在
- [x] 1.2 协议接受并保留新标识，同时兼容旧事件快照；用协议断言覆盖新旧两种消息
- [x] 1.3 建立 stable `v0.107.1` 事件目录索引，按 ID、标题和页面选项覆盖匹配；用 Agent 断言覆盖稳定 ID、旧标题和多页面事件

## 2. 确定性事件分析

- [x] 2.1 解析生命、最大生命、金币、删牌、升级、降级、变化、附魔、卡牌、遗物、药水、负面牌、战斗和随机性；用代表事件断言结构化效果
- [x] 2.2 根据当前资源生成逐项收益、代价、风险、可执行性和规则分数；断言致死与金币不足选项不可推荐
- [x] 2.3 用规则排序替换默认第一项保底；断言可靠规则无 LLM 可工作、全未知时返回无可靠建议

## 3. LLM 与界面

- [x] 3.1 LLM 输入增加事件目录匹配、逐项分析和允许索引；断言模型越界时回退规则结果
- [x] 3.2 主面板逐选项显示收益、代价、风险和规则来源；静态检查确认不解析或发明未知奖励
- [x] 3.3 桌宠显示首选收益、风险、知识版本与匹配来源；静态检查确认沿用现有建议卡和 advising 姿态

## 4. 文档与验证

- [x] 4.1 更新 README、功能状态、数据契约、Windows 验收与事件 TODO，注明 66 条事件覆盖及数据边界
- [x] 4.2 运行 `node_modules/.bin/openspec validate codex-event-choice-rules --strict` 并修复规范错误
- [x] 4.3 运行 `git diff --check` 并修复差异格式错误
- [ ] 4.4 运行最小相关 Harness 与 Mod 构建/真机验证并记录结果
