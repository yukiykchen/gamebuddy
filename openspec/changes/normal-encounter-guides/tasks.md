## 1. 遭遇识别与匹配

- [x] 1.1 将非 Boss/Elite 的活动战斗识别为 Normal，并在签名中支持普通战；新增 Harness 断言覆盖普通地图战和事件战
- [x] 1.2 Codex 上下文按 Normal→Monster 映射并依据实际敌人组合选择遭遇；新增共享怪物与完整组合匹配断言

## 2. 普通怪物策略

- [x] 2.1 基于确定机制标签生成摘要、牌组检查、目标优先级、危险、建议和常见失误；断言所有字段均非空且不包含伤害推算
- [x] 2.2 为社区档案和机制推导分别标记 `basis`、`reviewStatus`、来源与置信度；断言普通攻略不冒充社区复核

## 3. 桌宠与生命周期

- [x] 3.1 桌宠为 `normal` 显示“小怪攻略”标题、短提示和机制推导来源，并确认沿用现有关闭控件和 advising 姿态
- [x] 3.2 清理同场重复判断并验证普通战内状态更新不重复弹窗、战斗结束后下一场可以再次显示

## 4. 文档与验证

- [x] 4.1 更新 README、功能状态与数据契约，注明 63 个普通遭遇的机制覆盖和策略来源边界
- [x] 4.2 运行 `node_modules/.bin/openspec validate normal-encounter-guides --strict` 并修复规范错误
- [x] 4.3 运行 `git diff --check` 并修复差异格式错误
- [ ] 4.4 运行最小相关校验 `node harness/validate-agent.js`，记录结果
