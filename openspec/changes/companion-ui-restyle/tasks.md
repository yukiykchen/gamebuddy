## 1. 主窗口结构

- [ ] 1.1 把 `src/index.html` 改成顶栏 HUD（角色、层数、生命、金币、本局 SL、连接状态）+ 中文页签（战斗 / 选牌 / 路线）+ 主内容区，删除厚侧栏、编号导航和引擎广告卡。点页签仍能切到对应视图
- [ ] 1.2 重写 `src/styles.css` 夜石 / 羊皮纸 / Ember / Heart / Moss token；标题用宋体族，界面用 PingFang；去掉全大写英文眉题。`npm run test:syntax` 通过
- [ ] 1.3 调整 `src/renderer.js` 的战斗 / 选牌 / 休息 / 路线模板：当前建议放在主内容开头，等待文案说明下一步做什么。有休息或选牌建议时主窗口能读到标题和理由

## 2. 桌宠票券

- [ ] 2.1 把建议卡和精英攻略改成角色左侧短票券：类型标记、标题、最多三行理由、最多两组要点；宠物身体不被挡住。关闭、拖拽、点击打开主窗口仍可用
- [ ] 2.2 状态条改成角色脚下细条，保留 LIVE/等待、姿态和 `SL n`；不再用宽边框芯片。`src/pet.js` 缓存刷新后能看到数字
- [ ] 2.3 如需缩小建议卡窗口，只改 `main.js` 尺寸，角色安全区仍约 214×242

## 3. 验证

- [ ] 3.1 运行 `npm run test:syntax` 并通过
- [ ] 3.2 在 Electron LIVE 或 Demo 中打开战斗、选牌、休息、路线：主窗口 HUD 有数字，桌宠票券短且角色可见
