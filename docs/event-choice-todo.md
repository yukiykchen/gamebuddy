# 事件选择规则库状态

## 目标

事件触发后，GameBuddy 不再推荐地图路线，而是根据当前事件的真实规则、玩家状态和后续风险，给出可解释的选项建议。

Kimi 只负责结合上下文进行取舍，不负责凭记忆猜测 STS2 的事件规则。

## 当前状态

- [x] Mod 采集事件标题、描述和可用选项。
- [x] Agent 使用 `event_choice` 任务，并优先于地图路线和休息处建议。
- [x] Kimi 事件选择请求已接通。
- [x] 界面展示 Agent 分析中的状态。
- [x] 已从本地 `sts2.dll` 确认可提取事件类、选项 ID、动态数值和执行效果。
- [x] 已接入 66 条 Codex 事件、页面匹配和确定性效果分析器。
- [x] Kimi 请求失败时使用安全门禁后的规则首选；规则无法区分时不再默认第一项。

## 实施清单

### 1. 版本和数据来源

- [x] 固定当前事件规则基线为 stable `v0.107.1`。
- [x] 以本地游戏运行时文本和 Spire Codex 反编译/本地化数据为机制来源。
- [x] 推荐记录游戏版本、匹配方式、完整度和来源。
- [ ] 发布构建时记录本机 `sts2.dll` 的 SHA-256，并与数据快照自动比对。

### 2. 事件规则提取

- [x] Bridge 尽力读取事件 ID、页面 ID 和选项 ID，失败时按显示文本兼容。
- [x] 运行时按钮文本提供本次金币、生命损失和最大生命损失等动态数值。
- [x] 分析生命、最大生命、金币、卡牌、遗物、药水、负面牌、战斗和后续页面。
- [x] 读取目录前置条件和运行时锁定状态，推导致死、资源不足和随机结果范围。
- [ ] 提取选项的 HoverTip 关联内容，用于识别遗物、卡牌、药水和附魔。

### 3. 规则库格式

- [x] 使用 `events.json` + `event-rules.js` 作为版本化目录和分析层。
- [x] 目录记录 66 个事件的标题、页面和选项列表。
- [x] 每次推荐输出确定效果、代价、可执行性、风险等级、置信度和来源。
- [x] 对无法确认的效果标记 `unknown`，禁止把未知内容写成确定事实。
- [x] 同一分析路径覆盖全部可见事件选项，而非只维护少量事件白名单。

### 4. Mod 数据契约

- [x] 上报可读取的事件 ID 和当前页面 ID，同时保留显示标题。
- [x] 上报可读取的选项 ID、锁定状态和运行时动态效果文本。
- [x] 保持向后兼容；旧版本 Mod 缺少字段时按文本分析并标记不确定性。
- [x] 更新 `docs/data-contract.md`，记录新增字段和事件状态变化规则。
- [ ] 补充独立的 HoverTip 结构；当前遗物远古选项继续使用已采集的动态遗物描述。

### 5. 确定性分析器

- [x] 先判断锁定、金币不足和明确致死，再进入效用排序。
- [x] 计算明确即时收益与代价，并保留随机范围和未知项。
- [x] 根据当前生命、最大生命、金币、遗物、药水和卡组评估选项价值。
- [x] 将生存风险置于普通收益之前。
- [x] 识别负面牌代价、遗物价值和后续战斗影响；细粒度流派协同仍由 LLM 复核。
- [x] 输出每个选项的结构化利弊、风险、规则分数和置信度。

### 6. Kimi Agent

- [x] 将规则匹配、逐项分析和原始事件文本一起发送给 LLM。
- [x] 明确要求 LLM 只能使用已提供事实，不得补写未知效果或随机奖励。
- [x] LLM 返回推荐索引和局面理由；逐项评估、置信度及未知风险由规则层提供。
- [x] 程序先过滤锁定、资源不足和致命选择，再由 LLM 在允许索引中权衡。
- [x] 规则缺失时显示资料不完整，请求失败时回退规则，不再默认第一项。
- [x] 保留事件分析、超时和失败状态。

### 7. 测试和验收

- [ ] 为固定事件建立状态 fixture，覆盖健康、残血、金币不足、卡组协同和潜在致死场景。
- [ ] 验证规则库效果与反编译实现一致。
- [ ] 验证事件页面变化后会重新分析，而不是沿用旧建议。
- [ ] 验证地图打开、战斗开始和事件结束时任务切换正确。
- [ ] 验证 Kimi 不可用时不会产生未经证实的确定性建议。
- [ ] 运行 `npm run harness:validate-all`、语法检查、Mod 编译和真实游戏联调。
- [ ] 重新安装 Mod，并确认游戏目录中的 DLL 与构建产物一致。

## 验收标准

完成后，事件建议必须满足：

1. 推荐依据包含可追溯的真实事件效果，而不是只引用按钮文案。
2. 致死、最大生命损失、诅咒和战斗等高风险选项会被明确标记。
3. Kimi 只负责结合玩家状态进行权衡，不能凭空发明事件效果。
4. 无法确认规则时，界面必须明确提示不确定性，而不是给出伪确定答案。
5. 规则库与游戏版本不一致时，系统能识别并降低置信度或停止使用旧规则。
