# GameBuddy 功能实现状态

最后更新：2026-09-22
项目版本：`0.1.0`
策略数据目标版本：《杀戮尖塔 2》stable `v0.107.1`

## 状态定义

- **已实现**：代码路径完整，可通过 Replay 数据使用。
- **已实现，待真机验收**：代码已完成，但仍需在安装游戏的 Windows 环境记录端到端结果。
- **部分实现**：已有界面或底层数据，但用户目标尚未完整交付。
- **未实现**：代码中没有可用实现。
- **产品边界**：明确不应实现，避免误解为遗漏。

## 用户功能

| 功能 | 状态 | 实现说明 | 当前限制 |
| --- | --- | --- | --- |
| Electron 主面板 | 已实现 | 战斗、选牌、地图和商店工作台；休息处与事件建议复用地图页展示 | 尚未进行本轮 Windows DPI 和多显示器验收 |
| 悬浮桌宠 | 已实现 | 独立透明窗口、置顶、拖动、托盘显隐、单句决策卡、可展开理由、跟随真实请求的 LLM 思考动画与实时用时，完成后在详情保留最终耗时 | 桌宠角色资源仍需持续打磨；新卡片尚待 Windows 多显示器实机验收 |
| 实时状态展示 | 已实现，待真机验收 | 展示角色、Act、楼层、生命、能量、金币、手牌、敌人、地图等 | 正确性依赖 Mod 对当前游戏版本的字段适配 |
| 卡牌奖励建议 | 已实现 | 分析全部候选牌，可选择 `SKIP`；桌宠默认显示“拿哪张牌”，展开后显示选择理由、优点、风险、来源和置信度 | 规则权重尚未用大规模历史奖励决策回测校准 |
| 地图路线建议 | 已实现 | 仅在当前分叉时按不同可点击入口比较；桌宠默认显示具体入口，展开后查看安全、平衡、成长三档依据；单一路径不触发 LLM | 未知节点无法预知结果；最多枚举 256 条路线，截断会提示；证据接近时保留暂时首选并降低置信度；评分不是胜率或伤害预测 |
| 休息处建议 | 已实现 | 社区策略 Skill 比较预计回血与逐张升级差值，结合使用频率、牌组协同、完整资源、地图和近期强敌给出具体目标与理由 | 回血量按当前规则估计，特殊遗物修正仍依赖状态数据完整度；策略锁定 stable `v0.107.1` |
| 问号事件建议 | 已实现 | 匹配 66 条 Codex 事件及当前页面，分析每个真实选项的收益、代价、风险、资源门槛和未知项，LLM 只在安全索引内复核 | 文本分析器未识别的特殊效果会降低置信度；新 Bridge 标识仍待真机验收 |
| 商店选择建议 | 已实现，待真机验收 | 展示全部在售卡牌、遗物、药水与删牌服务，进店时按当前金币一次生成最多 3 项的完整购物清单，并允许保留金币 | 同一次访问不因逐件购买重新调用模型；价格和库存依赖当前版本 Mod 字段，购买仍由玩家完成 |
| 小怪 / 精英 / Boss 攻略弹窗 | 已实现 | 所有战斗进入时自动展开一次；显示机制、招式、危险、牌组检查、目标顺序和失误；关闭后可在当前战斗从桌宠重新打开 | 普通战为机制推导；社区策略必须随 Early Access 补丁复核 |
| 战斗出牌顺序 | 未实现 | 当前战斗页只同步状态 | 尚未建立合法行动枚举、能量模拟和状态转移模型 |
| 自动点击 / 自动出牌 | 产品边界 | Mod 与桌面端均保持只读 | 不会加入控制游戏的接口 |
| 多人模式专项策略 | 未实现 | 协议未阻止多人状态，但没有合作策略层 | 状态语义、目标选择和团队协同未验证 |

## 数据采集与协议

| 能力 | 状态 | 已覆盖内容 | 主要风险 |
| --- | --- | --- | --- |
| `GameBuddyBridge` Mod | 已实现，待真机验收 | 当前局、玩家、地图、战斗牌堆、敌人、意图、奖励界面、商店库存与价格 | 游戏更新可能改变 Godot 节点或 Harmony Hook 目标 |
| 状态协议 | 已实现 | `gamebuddy.state.v1` 校验、快照请求和重复消息处理 | 协议升级需要同步 Mod、Harness 和 Electron |
| 事件协议 | 已实现 | 战斗开始/结束、回合开始、地图、休息处、卡牌奖励、事件房和商店事件 | 真实事件时序仍需完整对局录制验证 |
| Observation Store | 已实现 | 最新状态、事件历史、序列号、年龄和 freshness | 超过 5 秒无状态时标记 stale，不生成新的过期建议 |
| 自动重连 | 已实现 | Bridge 断开后每 2.5 秒重试 | 未提供 UI 内自定义重连间隔 |
| Replay Bridge | 已实现 | 固定 fixture 循环推送并推断生命周期事件 | fixture 不能替代真实版本验收 |
| 真实对局录制 | 已实现 | 状态和事件分文件保存，可直接回放 | 录制内容需要人工确认是否适合提交仓库 |

## Agent 与知识库

| 能力 | 状态 | 数据与决策方式 | 降级行为 |
| --- | --- | --- | --- |
| 任务编排 | 已实现 | 奖励优先，其次商店、事件、休息处和地图分叉；按状态签名去重 | 离开场景会取消过期请求并清除对应建议 |
| 卡牌规则评分 | 已实现 | 卡牌机制、牌组缺口、协同标签、章节、未来威胁和社区先验 | Spire Codex 失败时使用 Bridge 和本地知识 |
| 路线规则评分 | 已实现 | 收益与风险拆分，安全/平衡/成长三个视角比较当前不同入口，考虑生命、金币、遗物、删牌、升级、相邻节点顺序、后续分叉和连续战斗 | LLM 不可用时直接展示规则首选；危险生命时拒绝明显低于安全首选的模型选择；不再输出“路线等价任选” |
| 休息处规则评分 | 已实现 | `rest-site-strategy` Skill 识别升级前后质变、数值增量、使用频率、牌组协同、生命安全线和近期威胁 | LLM 不可用时直接展示规则首选；社区经验不是最优性的因果证明 |
| 事件规则评分 | 已实现 | 运行时动态文本 + stable `v0.107.1` Codex 决策树；生存/资源门禁后比较删牌、升级、遗物、金币等收益 | 全部效果未知时不发布伪确定首选；玩家可直接阅读游戏原始选项 |
| 商店规则评分 | 已实现，待真机验收 | 实际价格与预算约束下组合卡牌、遗物、药水和删牌，结合牌组协同、药水槽、后续地图、已知 Boss / 精英及保留金币价值 | 未知价格不可购买；LLM 只能在合法组合中复核，并一次返回完整清单 |
| LLM 复核 | 已实现，可选 | `gpt-5.5` 默认，Responses API；只能选择给定候选 | API 错误、超时或无 Key 时退回规则 |
| Spire Codex 接入 | 已实现 | 卡牌、遗物、药水、怪物、遭遇、能力与 pick coach | 请求失败不会阻塞 UI |
| 全卡评价档案 | 已实现 | 每张卡包含 rank、中文总结、适用/避用条件和来源元数据 | 需要随 stable / beta 版本重新生成 |
| 遭遇策略 | 已实现 | 63 个普通遭遇使用机制推导；12 Boss + 12 精英使用带来源、置信度和版本的社区档案 | 机制推导不等同于人工攻略；社区综合也不等同于官方结论 |

## 界面显示与实际决策的关系

```text
Bridge 状态
  ├─ 战斗存在 -> 显示真实手牌与敌人，并为小怪/精英/Boss 生成一次性攻略弹窗
  └─ 非战斗
      ├─ 卡牌奖励存在 -> card_reward
      ├─ 商店库存存在 -> shop_choice
      ├─ 事件选项存在 -> event_choice
      ├─ 休息处选择未完成 -> rest_site
      └─ 下一步有分叉（含火堆选完后仍在格子上） -> map_route
```

模型不会直接读取游戏进程，也不会自行猜测可选动作。Agent 先根据协议状态生成候选，再把候选和上下文交给模型复核。

## 验证现状

仓库已经提供以下验证入口：

- `npm run harness:validate-all`：脚本语法、协议、fixture、录制文件和 Mod 文件形状。
- `npm run harness:smoke`：Replay WebSocket 握手和第一份状态。
- `npm run harness:smoke:lifecycle`：战斗、回合、战斗结束和地图事件。
- `npm run harness:inspect -- --once`：真实 Bridge 无界面诊断。
- `docs/windows-validation.md`：真实游戏端到端验收清单。

当前实现不能仅凭 Replay 测试宣称真实 Mod 已完成验收。发布前仍需在 Windows stable `v0.107.1` 至少记录一次：Mod 加载、合法快照、战斗更新、奖励识别、商店完整清单及购买时不重复分析、地图路线、休息处、遭遇攻略、断线重连和退出行为。

## 下一阶段建议

优先级从高到低：

1. 完成 Windows stable `v0.107.1` 全链路验收并保留脱敏录制 fixture。
2. 用真实奖励选择与最终结果回测卡牌评分，校准规则权重和置信度。
3. 为 Early Access 版本变更建立知识库差异检查，避免 stable / beta 混用。
4. 扩充遭遇弹窗的实时阶段提示，使建议能结合当前回合和敌方意图。
5. 战斗出牌 Agent 只有在合法行动枚举、能量消耗、目标、随机效果和状态转移均可验证后再启动。
